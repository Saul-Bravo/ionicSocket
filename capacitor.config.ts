import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicSockets',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
